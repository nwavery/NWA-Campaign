<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>${process.env.PRODUCTION_URL || 'http://localhost:3000'}</loc>
    <lastmod>${new Date().toISOString().split('T')[0]}</lastmod> <!-- Use current date -->
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>

  <!-- Add other pages here if created later -->
  <!--
  <url>
    <loc>${process.env.PRODUCTION_URL}/about</loc>
    <lastmod>YYYY-MM-DD</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>
  -->
</urlset> 